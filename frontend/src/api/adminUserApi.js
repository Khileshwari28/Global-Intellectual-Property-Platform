import axiosClient from "./axiosClient";

// No AdminUserController endpoints found in the components shared so far.
// Add functions here once you send admin-user-management components
// (e.g. list users, change a user's role, disable a user, etc.)